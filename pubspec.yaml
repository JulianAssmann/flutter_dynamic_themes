name: dynamic_themes
description: Allows you to specify a number of color themes for the user to choose from. Persits the selected theme across app restarts.
version: 1.0.0
repository: https://github.com/JulianAssmann/flutter_dynamic_themes

environment:
  sdk: '>=2.12.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: ^2.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter: